# 코드 리뷰: 단위 변환 매크로 모듈

## 개요
사용자가 제공한 VBA 모듈은 워크시트 템플릿을 생성하고, 원 단위를 천원·백만원 단위로 변환하며, 합계 맞춤과 버튼 배치를 자동화합니다. 입력값 검증과 누락 알림, 합계 대비 차이 계산까지 포함되어 실무용으로 구성된 코드입니다.

## 강점
- **양식 생성과 초기화 일관성**: `MakeTemplate`/`SetupButtons`/`ApplyFreeze` 흐름을 재사용하여 새 시트, 초기화, 추가 열 삽입 등에서 동일한 레이아웃을 유지하도록 설계했습니다.
- **입력 정제 및 오류 안내**: `ValidateInputAndGetEndRow`가 숫자/공백/누락을 구분해 자동 정리 여부를 묻고, 누락 행을 기억해 중복 알림을 줄이는 등 사용자 경험을 배려했습니다.
- **합계 보정 자동화**: `ConvertAndAdjust`가 잔여 오차(`err`)를 기준으로 보정 대상을 정렬하고, `MakeTotals`에서 차이를 원 단위로 보여줘 검토를 돕습니다.

## 개선/주의 사항
1. **라운딩 방식이 은행가 반올림(짝수 올림)으로 고정됨**: `WorksheetFunction.Round`는 Excel의 은행가 반올림을 사용해 `1.5 → 2`, `2.5 → 2`가 됩니다. 천원/백만원 변환과 합계 맞춤에서 기대하는 일반 반올림(0.5 올림)과 달라 사용자가 수기 검증할 때 차이를 낼 수 있습니다. `Round` 대신 `WorksheetFunction.RoundUp/Down` 조합이나 사용자 정의 Half-Up 함수를 사용해 정책을 명확히 반영하는 것이 안전합니다.
2. **음수 표현을 제한적으로만 처리**: `CleanNumericString`는 쉼표·공백·"원"을 제거하지만 괄호식 음수(`(1,000)`)나 선행/후행 `+`·`-` 기호 제거를 하지 않습니다. 재무 데이터에서 흔한 괄호 음수나 `- 1,000` 입력 시 숫자 판정이 실패해 사용자가 계속 수정해야 합니다. 괄호를 `-`로 치환하고 부호 주변 공백을 정리하면 입력 호환성이 높아집니다.
3. **선택된 시트 의존성**: `YG_Tool_Convert`, `YG_Tool_Clear`, `YG_Tool_AddDividerColumn`가 `ActiveSheet` 기반으로 동작해, 다른 통합문서나 보호된 시트가 활성화된 상태에서 눌리면 MsgBox 후 종료하지만, `YG_Tool_Init`처럼 시트 탐색을 위한 `Activate` 호출이 포함되어 있습니다. 이벤트 매크로나 다른 작업과 동시에 실행될 경우 의도치 않게 사용자의 포커스를 이동시킬 수 있으니, 필요한 범위만 `With ws`로 처리하고 선택/활성화를 최소화하는 것이 안정적입니다.

## 제안 요약
- 변환·보정에서 사용할 반올림 정책을 명시하고, Half-Up 구현을 검토합니다.
- 괄호식 음수·부호 포함 문자열을 허용하도록 `CleanNumericString`를 확장합니다.
- 활성 시트 이동을 최소화해 사용자 포커스가 바뀌지 않도록 리팩터링을 고려합니다.
